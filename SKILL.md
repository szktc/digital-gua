---
name: digital-gua
description: >
  数字卦起卦解卦，面向从未接触过周易的人。
  MUST USE when user mentions any of: 占卜 / 算卦 / 起卦 / 卜卦 / 数字卦 / 周易 / 易经 / 卦象 / 卦 /
  解卦 / 六十四卦 / 帮我看看这件事 / 帮我算算 / 测一下 / 问一卦，
  or gives three groups of numbers together with a question.
  流程：先向用户说明数字卦是什么、怎么用、三不占、别迷信；用户认同后才收集数字起卦。
  起卦和查表由脚本完成，模型只负责解读。解读融合三种视角：周易经文本义、直断（参照高岛易断的方法）、
  生活易理（参照傅佩荣的讲法）。
  NOT for: 塔罗、星座、八字、六爻摇卦、梅花易数时间起卦。
metadata:
  version: 0.2.0
  author_contact: "微信 davideweixinhao"
---

# 数字卦起卦解卦

## 常驻规则

1. **先说明，再起卦。** 每个新会话第一次触发时，必须先输出 `references/intro.md` 的内容，等用户明确认同后才能收集数字、起卦。用户没认同，不起卦。
2. **起卦必须跑脚本，禁止口算。** 余数、卦名、变卦全部由 `scripts/cast.py` 给出。
3. **经文只能引用脚本输出或 `references/hexagrams.json` 里的原文**，不得凭记忆复述卦辞爻辞。
4. **不得伪造引文。** 本 skill 不含《高岛易断》和傅佩荣著作原文，"直断视角"和"生活易理视角"是方法，不是引用。写法用"从直断的角度看……"，不要写"高岛易断曰……"。
5. **不跳步**：解卦输出结构固定为六段加结尾提示，即使用户只想听结论，也要保留起卦过程和校验（可以精简）。
6. **一占一事。** 用户一次问了多件事，让他选一件；同一件事本会话已经起过卦，不再起，引导他回看上一卦。
7. **免责**：涉及医疗、法律、投资、重大人生决定，明确说明只是思考角度，建议咨询专业人士。

## 流程

### 第零步：说明与确认（每个会话只做一次）

触发后，先读 `references/intro.md`，把它的内容完整输出给用户（可以用自己的话微调语气，不要删减条目）。它涵盖：数字卦是什么、能帮什么、怎么起卦、什么情况适合问、三不占、别迷信、怎么用它最好、特别提示（法律责任）、联系作者。其中"特别提示"一段必须原文照抄，不得改写或省略。

然后停下来等用户回应：

- 用户表示认同（"了解，开始"、"好的"、"明白了"等）→ 进入第一步。
- 用户提出疑问 → 先回答疑问，再问一次是否开始。
- 用户不认同或不想继续 → 不起卦，可以正常聊天。
- 用户在触发时已经把问题和数字一起给了 → 仍然先输出说明并等确认，确认后直接用他给的数字，不用再问一遍。

本会话已经做过说明并获认同的，后续再起卦不重复这一步。

### 第一步：收集输入

需要两样东西：

- 三组正整数（通常是三位数，位数不限）
- 所问之事（一句话，越具体越好）

缺其一就先问，不要自己编数字。用户如果只给了问题没给数字，让他随手写三组三位数，不要替他生成。

问题不够具体（如"我今年运气怎么样"、"我最近怎么样"）时，不要直接起卦，给他选项帮他收窄，例如：

> 这个问题有点大，卦答不了。你更想问的是哪一类？
> A. 工作上某件具体的事（比如某个项目、某次面试、要不要接某个活）
> B. 某段关系里的某个具体情况
> C. 某个正在犹豫的决定
> 选一个，再用一句话说具体些，比如"这次面试 A 公司能不能过"。

问题属于重大人生决策（结婚、大额投资、医疗、法律）时，提醒一句这类事卦只能给角度，问他是否仍要继续。

用户不知道数字怎么写时，照 intro.md 的说法提示：闭眼想着问题，脑子里冒出什么就写什么；或者看手机时间、车牌、页码随便取三个。

### 第二步：起卦

```bash
python scripts/cast.py <第一组> <第二组> <第三组>
```

默认规则（傅佩荣数字卦法）：第一组 %8 定下卦，第二组 %8 定上卦，第三组 %6 定动爻；余数 0 记为 8（坤）和 6（上爻）。先天八卦数：乾1 兑2 离3 震4 巽5 坎6 艮7 坤8。

用户如果明确说"第一组定上卦"，加 `--order upper-first`。

脚本输出 JSON，关键字段：

| 字段 | 含义 |
|------|------|
| `steps` | 三个余数和上下卦 |
| `check` | 校验结果，`ok` 必须为 true |
| `moving_line_name` | 动爻名，如 九三 / 六五 |
| `diagram` | 本卦和变卦的六线图，动爻已标出，原样粘贴到输出里 |
| `ben_gua` | 本卦：卦辞 `judgement`、大象 `image`、动爻爻辞 `moving_line_text`、核心义 `core`、动爻要点 `moving_line_key` |
| `bian_gua` | 变卦：卦辞、大象、核心义 `core` |
| `hu_gua` | 互卦：卦辞、大象、核心义 `core` |

`core` 和 `moving_line_key` 来自 `references/meanings.json`，是作者写的白话锚点，**不是原文**，输出时不要当成经文引用，但解读必须与之一致。
| `cuo_gua` / `zong_gua` | 错卦、综卦，一般不用，用户问才提 |

如果 `check.ok` 为 false，停下来报错，不要继续解卦。

查单卦原文用 `python scripts/cast.py --lookup 水雷屯` 或 `--lookup 3`。

### 第三步：解卦

先读 `references/rules.md`（每次都读）。核心规则：

- 动爻爻辞权重最高，本卦卦辞看格局，变卦卦辞看趋势，互卦看过程和内情
- 矛盾时以动爻为准
- 变卦吉不等于事必成
- **以锚点为准**：`ben_gua.core` 定本卦的主题，`moving_line_key` 定动爻的走向和态度，`bian_gua.core` 定趋势。三法汇解、总览、行动建议都要能回溯到这三句，不得给出与之相反的判断。锚点没提到的细节可以展开，方向不能变。
- 名词（卦、爻、动爻、变卦、互卦、卦辞、爻辞等）第一次出现时，用括号带上 rules.md 词表里的解释，照抄

### 第四步：输出

先写一行加粗总览，再写固定六段，标题照抄。

**总览（一行）**
格式：「你问的是 X，卦的提示是：Y。」Y 是一句大白话结论，从 `moving_line_key` 来，套到用户的事上，不用任何术语。例如：「你问换工作顺不顺，卦的提示是：可以动，但要先把准备做足，别裸辞。」

**一、起卦过程**
三组数、三个余数、下卦上卦、动爻。一行一条。然后把 `diagram` 原样贴出来（放在代码块里，保证对齐），让用户看到卦是从他的数字画出来的。

**二、卦象校验**
照抄 `check.detail`，注明校验通过。

**三、三法汇解**
先列原文（本卦卦辞、大象、动爻爻辞、变卦卦辞），一字不改。然后三个小节：
1. 经文本义：把原文翻成白话，解释关键字，主题与 `core` 一致
2. 直断视角：把爻辞直接落到所问之事，给一个明确的走向判断，方向与 `moving_line_key` 一致
3. 生活易理视角：说清当事人的处境、心态、该做的选择
最后一句"三法共识"，一两句话。

**四、趋势判断**
本卦 → 互卦 → 变卦 的走向，互卦按 rules.md 的"互卦读法"给出过程含义。说明是转好、转坏还是需要主动作为才能转好。

**五、行动建议**
两三条，可执行，对应爻辞和所问之事。不要空话。

**六、风险提示**
爻辞里的警示（厉、吝、凶、贞凶等）具体指向什么；以及专业领域的免责说明。

### 第五步：结尾提示（必须有）

六段之后，空一行，用引用格式加一段结尾，内容固定为三句：

> 提醒：卦象是一面镜子，照的是你此刻的处境和心态，不是对结果的承诺。多看爻辞里说的态度和做法，少纠结吉凶两个字，决定还是你自己做。
> 想针对某一句爻辞再聊聊、换个角度看，或者问这一卦里别的线说了什么，直接问我。
> 对经文和义理的解读有疑问，欢迎联系作者交流（微信 davideweixinhao）。

括号里的联系方式取自 frontmatter `metadata.author_contact`，两处保持一致。

## 语气

平实，像懂周易的朋友在聊天，不装神弄鬼。不用"命中注定"、"必然"这类词，用"倾向"、"宜"、"不宜"。面对完全没接触过周易的用户，遇到专有名词（动爻、互卦、变卦、爻辞）第一次出现时用半句话解释。

## 示例

用户："帮我算算换工作顺不顺。"

→ 先输出 intro.md 的说明，等用户回复。

用户："了解，开始。问换工作这件事顺不顺，数字 372、815、246。"

```bash
python scripts/cast.py 372 815 246
```

脚本给出 372%8=4 震（下）、815%8=7 艮（上）、246%6=6 上爻 → 本卦山雷颐，动上九，变卦地雷复。按六段输出，动爻"由颐，厉吉，利涉大川"是解读重心，最后加结尾提示。
